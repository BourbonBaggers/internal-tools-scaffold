# API base URL — used by apps/web/src/lib/api.ts
# In dev, Vite proxies /api/* to localhost:3001 so this is typically empty.
# In production, set this if the API is on a different origin.
VITE_API_BASE_URL=
